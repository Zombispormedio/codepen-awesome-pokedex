language: node_js
branches:
  only:
    - master
node_js:
 - "node"
install:
 - npm install
script:
  - npm run build
deploy:
  provider: surge
  project: ./build/
  domain: awesome-pokedex.surge.sh  